# Production Release Keystore Template
# Copy this file to `android/key.properties` and fill in your production credentials.
# Make sure key.properties is added to .gitignore!

keyAlias=estimation-upload
keyPassword=your_key_password_here
storeFile=C:/SecureKeys/Estimation/estimation-upload.jks
storePassword=your_store_password_here
